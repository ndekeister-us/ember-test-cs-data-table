<table class="cs-data-table">
  <thead>
  <tr>
    {{#each @columns as |column|}}
      <th>{{column.label}}</th>
    {{/each}}
  </tr>
  </thead>
  <tbody>
  {{#each @data as |item|}}
    {{yield (hash row=(component "cs-data-table/-row" item=item))}}
  {{/each}}
  </tbody>
</table>