<tr ...attributes>
  {{#each (sort-by "order" @columns) as |column|}}
    {{yield (hash
              cell=(
                component "cs-data-table/-row/-cell"
                column=column
                isCustomizingColumns=@isCustomizingColumns
                item=@item
              )
            )
    }}
  {{/each}}
</tr>